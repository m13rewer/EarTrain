import { ShortCutsPipe } from './short-cuts.pipe';

describe('ShortCutsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortCutsPipe();
    expect(pipe).toBeTruthy();
  });
});
